---
// CSS imports — Vite/Astro bundles these automatically
import "@usevyre/tokens/css";        // design tokens (CSS variables)
import "@usevyre/react/styles";      // component styles (framework-agnostic)
import "@/styles/docs.css";       // docs site-specific styles
import EarlyAccessBanner from "@/components/layout/EarlyAccessBanner.astro";
import Analytics from "@vercel/analytics/astro";

export interface Props {
  title:        string;
  description?: string;
  /** Standalone/preview pages (e.g. example apps) hide the early-access banner. */
  hideBanner?:  boolean;
}

const { title, description = "AI-native design system for React and Vue. Zero hallucinations.", hideBanner = false } = Astro.props;
const canonical = new URL(Astro.url.pathname, Astro.site);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonical} />
    <title>{title} — useVyre</title>

    <!-- FOUC prevention: apply saved theme before first paint -->
    <script is:inline>
      (function () {
        try {
          var saved = localStorage.getItem("vyre-theme");
          if (saved === "dark" || saved === "light") {
            document.documentElement.setAttribute("data-theme", saved);
          }
          // If no saved preference, CSS @media handles it — no attribute needed
        } catch (_) {}
      })();
    </script>

    <!-- Favicons -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta name="theme-color" content="#7c3aed" />

    <!-- Open Graph -->
    <meta property="og:title"       content={`${title} — useVyre`} />
    <meta property="og:description" content={description} />
    <meta property="og:type"        content="website" />
    <meta property="og:url"         content={canonical} />

    <!-- Vercel Web Analytics (placed in <head> per Vercel docs) -->
    <Analytics />
  </head>
  <body>
    {!hideBanner && <EarlyAccessBanner />}
    <slot />
  </body>
</html>
