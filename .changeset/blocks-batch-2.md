---
"@usevyre/ai-context": minor
---

Add 6 more composition Blocks for common app screens: AppShell (sidebar + app bar layout), DataTablePage (toolbar + table + pagination), FormPage (validated create/edit form), PageHeader (breadcrumb + title + actions), ConfirmDialog (destructive modal), and ItemList (rows with media + actions). Like the existing blocks, they're copy-paste source injected into the AI context and browsable with live previews at `/docs/blocks`. useVyre now ships 11 blocks.
