---
"@usevyre/react": patch
---

The React bundle now ships with the `"use client"` directive, so components work in React Server Components / Next.js App Router out of the box — consumers no longer need to wrap useVyre components or add `"use client"` themselves.
