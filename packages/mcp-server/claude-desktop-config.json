{
  "_comment": "Add this to your Claude Desktop config at ~/Library/Application Support/Claude/claude_desktop_config.json",
  "mcpServers": {
    "usevyre-context": {
      "command": "npx",
      "args": ["-y", "@usevyre/mcp-server"],
      "description": "useVyre design system context — component props, anti-patterns, and examples"
    }
  }
}
